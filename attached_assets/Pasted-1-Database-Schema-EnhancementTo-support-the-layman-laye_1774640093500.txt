1. Database Schema EnhancementTo support the "layman" layer, the chart_of_accounts table needs to store the human-readable context for each account.Task: Update shared/schema.ts to add laymanDescription: text("layman_description") to the chart_of_accounts table.Seeding: Update the DEFAULT_COA constant in server/accounting-engine.ts to include these descriptions. Examples:5010 (COGS - Food): "Money spent on flour, butter, yeast, and other ingredients to make our products."6010 (Labor - Wages): "Straight pay for our team's hours worked, as recorded in the time-clock."2. Engineering the Three-Layer Audit EngineA new service, server/audit-lineage-engine.ts, will provide the intelligence for the drill-down panel.Layer 1 (Quantitative): Perform a deep join between ledger_lines, journal_entries, and chart_of_accounts to pull every raw transaction contributing to a specific COA code within the selected date range.Layer 2 (The Auditor): Implement a "Double-Counting" scan. The engine will look for entries with identical amounts (e.g., $450.00) that occur within ±3 days of each other in different categories (like "Supplies" vs "Misc"), flagging them as high-risk for duplicate booking.Layer 3 (Accrual Tracking): Filter for entries where journal_entries.isNonCash is true. These are "Ghost Entries"—expenses like depreciation or accrued labor that lower your profit without actually moving cash from your checking account today.3. AI Narrative GenerationBy leveraging your existing OpenAI pattern in server/ghost-accountant.ts, we will generate a trend analysis for the specific account being viewed.Context: Pass the list of vendor names, transaction frequencies (e.g., "US Foods 4x this month"), and total spend to the AI.Prompting: Instruct Jarvis to explain the trend in plain English (e.g., "Your food cost is up 15% because we received an extra order from BakeMark on the 28th that usually lands in the following month").4. Ferrari-Themed UI: FinancialLineagePanel.tsxThis new component will use the Sheet (slide-out) pattern with a specialized "diagnostic" visual style.Visual Treatment: A "Carbon Fiber" dark mode aesthetic using a custom background class (e.g., bg-zinc-950/95 with a subtle grid pattern) to make it feel like a "look under the hood."Integration: Add cursor-pointer hover:ring-2 hover:ring-primary styles to the Revenue, Expense, and P&L cards in TheFirm.tsx. Clicking these will trigger the panel, passing the relevant COA codes (or categories) and the global date filter.5. API Endpoint SecurityThe new route GET /api/firm/audit/lineage will be restricted to users with the owner role, following the strict security patterns found in server/routes.ts.FeatureLayman BenefitFerrari EngineeringLayman DescriptionsUnderstand what an account actually means.Schema-driven contextual metadata.Constituent EntriesSee exactly where every cent went.Deep-trace ledger reconstruction.Duplicate AuditorPeace of mind that you aren't paying twice.Cross-account fuzzy matching logic.Accrual ImpactUnderstand why profit ≠ cash in bank.isNonCash flag visualization.AI NarrativeGet a summary without doing the math.Real-time OpenAI spending analysis.