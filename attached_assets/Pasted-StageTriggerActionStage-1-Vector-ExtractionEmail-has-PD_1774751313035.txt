StageTriggerActionStage 1: Vector ExtractionEmail has PDF/ImageExecute existing OCR/Vision logic to pull structured line items.Stage 2: Semantic HTML ParsingNo PDF foundPass messageBody (HTML/Text) to a specific LLM prompt designed to identify "Invisible Tables" and order summaries.Stage 3: URL Forensic AnalysisNo data in bodyUse Regex to locate external portal links (e.g., /view-order/) and flag for Stage 4.Stage 4: Headless ExtractionHigh-value external linkTrigger a Puppeteer/Playwright worker to visit the link and capture a DOM snapshot for AI parsing.2. Cross-Reference & Inference EngineIntegrate the email engine with the firmTransactions and chartOfAccounts tables to "fill in the blanks" when data is thin:Transactional Anchoring: Use the plaid_transaction_id or amount + date as a key to link a "Thin Email" to a specific bank debit.Historical Pattern Matching: If a $2,500 charge to "Phinney Design Group" is found but the receipt is just a "Thank You," Jarvis should query journalEntries for previous mappings to code 6100 (Professional Services) and propose an identical split with 95% confidence.Vendor Intelligence: Add a vendor_profiles layer that knows specific vendor behaviors (e.g., "Air France always sends receipts via link, not attachment") to skip Stage 2 and go straight to Stage 4.3. S-Corp Basis & Asset IntelligenceThe email engine must feed directly into the Asset Engine (server/asset-engine.ts) to optimize your tax position:CapEx Detection: Any email with a total >$2,500 automatically triggers isCapExCandidate().Prepaid Amortization Logic: If the engine detects "Retainer," "Annual Fee," or "Professional Services" (e.g., for WDR CPAs), Jarvis should not just post to the P&L; it should invoke the BasisAssessor to propose a Prepaid Amortization Schedule to spread the cost over 12 months.4. Proactive "Ghost" ActionsIntelligence is defined by Jarvis’s ability to resolve its own limitations:Draft-to-Action: If Stage 4 fails (e.g., login required), Jarvis generates a Gmail Draft addressed to the vendor: "Bear's Cup Bakehouse is missing an itemized invoice for Order #XYZ. Please reply with a PDF copy for our S-Corp records".Compliance Alerts: Instead of a generic error, generate a specific financialConsultation record with a "Critical" severity if a transaction >$15,000 is missing a receipt.5. Unified Reference ID ArchitectureTo prevent the "Production Doubles" bug, every extraction must use a strictly deterministic Source ID:RefID: gmail-msg-{messageId}Guard Logic: Before posting any entry from an email, the engine must check journalEntries where referenceId matches the Gmail ID to ensure Idempotency (never posting the same email twice).